{
  "name": "vestr-test-task",
  "version": "1.0.0",
  "description": "Express backend + React frontend with TypeScript",
  "scripts": {
    "install:all": "concurrently \"npm install --prefix backend\" \"npm install --prefix client\"",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix backend && npm run build --prefix client",
    "start": "npm start --prefix backend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}

